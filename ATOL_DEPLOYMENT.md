# 🚀 Инструкция по развертыванию на ATOL OPTIMA v7

## 📋 Оглавление
1. [Требования](#требования)
2. [Сборка приложения](#сборка-приложения)
3. [Подготовка ATOL OPTIMA v7](#подготовка-atol-optima-v7)
4. [Установка на ATOL](#установка-на-atol)
5. [Первый запуск и проверка](#первый-запуск-и-проверка)
6. [Тестирование функционала](#тестирование-функционала)
7. [Устранение неполадок](#устранение-неполадок)
8. [Администрирование](#администрирование)

---

## 🔧 Требования

### Для сборки (на вашем компьютере):
- **Node.js**: v18.x или выше
- **npm**: v7.x или выше
- **ОС**: Windows, macOS или Linux
- **Свободное место**: ~2 GB

### ATOL OPTIMA v7:
- **Процессор**: Intel/AMD x64
- **ОЗУ**: минимум 4 GB (рекомендуется 8 GB)
- **ОС**: Windows 10/11 (x64)
- **Свободное место**: ~500 MB на диске
- **Права**: Администратор Windows (для установки)

---

## 📦 Сборка приложения

### Шаг 1: Подготовка проекта

```bash
# Клонируйте репозиторий (если еще не клонирован)
git clone https://github.com/idrisovTamer/electron.git
cd electron-react-boilerplate

# Установите зависимости
npm install
```

### Шаг 2: Сборка для Windows

```bash
# Сборка production версии для Windows x64
npm run package:win
```

**Что происходит:**
- ✅ Компиляция TypeScript → JavaScript
- ✅ Сборка Webpack bundle
- ✅ Удаление dev-зависимостей (`npm prune --production`)
- ✅ Упаковка в NSIS installer
- ⏱️ Время сборки: ~2-5 минут

### Шаг 3: Проверка результата

После успешной сборки в папке `dist/` будет файл:

```
dist/
└── MyPOSApp-Setup.exe  (~150-180 MB)
```

---

## 🖥️ Подготовка ATOL OPTIMA v7

### Перед установкой проверьте:

#### 1. Версия Windows
```cmd
winver
```
- ✅ Должна быть **Windows 10** или **Windows 11**
- ✅ Архитектура **x64**

#### 2. Свободное место на диске
```cmd
wmic logicaldisk get size,freespace,caption
```
- ✅ Минимум **500 MB** свободного места

#### 3. Права администратора
- ✅ Установка требует прав администратора
- ✅ Попросите IT-специалиста или администратора

#### 4. Отключите антивирус (временно)
⚠️ Некоторые антивирусы могут блокировать установку Electron приложений

---

## 💾 Установка на ATOL

### Вариант 1: Установка с USB-флешки

#### Шаг 1: Копирование установщика
1. Скопируйте `MyPOSApp-Setup.exe` на USB-флешку
2. Вставьте флешку в ATOL OPTIMA v7

#### Шаг 2: Запуск установщика
1. Откройте **"Этот компьютер"**
2. Перейдите на флешку
3. **Правой кнопкой** на `MyPOSApp-Setup.exe`
4. Выберите **"Запуск от имени администратора"**

#### Шаг 3: Процесс установки
1. Появится окно NSIS installer
2. **Выберите путь установки** (по умолчанию: `C:\Program Files\MyPOSApp`)
3. Отметьте галочки:
   - ✅ **Создать ярлык на рабочем столе**
   - ✅ **Создать ярлык в меню Пуск**
4. Нажмите **"Установить"**
5. Дождитесь завершения (~30-60 секунд)

### Вариант 2: Установка по сети (если есть доступ)

```cmd
# На ATOL выполните:
net use Z: \\ваш-сервер\shared /user:username password
Z:
cd path\to\installer
MyPOSApp-Setup.exe
```

---

## 🚀 Первый запуск и проверка

### Запуск приложения

#### Способ 1: Ярлык на рабочем столе
Двойной клик на **"ATOL POS"**

#### Способ 2: Меню Пуск
1. Нажмите **Win**
2. Введите **"ATOL POS"**
3. Нажмите **Enter**

#### Способ 3: Из командной строки
```cmd
"C:\Program Files\MyPOSApp\MyPOSApp.exe"
```

### ✅ Проверка успешного запуска

После запуска вы должны увидеть:

1. **Полноэкранное приложение** (занимает весь экран)
2. **Главную страницу** с плитками тестов:
   - 📊 Большая таблица
   - 🧮 Сложные вычисления
   - 🎨 Canvas анимации
   - 👨‍🍳 Кухонный экран
   - ⚡ TanStack Query
   - ⌨️ Сенсорная клавиатура
   - 📅 Календарь

3. **Заблокированный выход**:
   - ❌ Alt+F4 не работает
   - ❌ Крестик закрытия не работает
   - ❌ F12 не открывает DevTools

### 📍 Расположение логов

Логи приложения сохраняются автоматически:

```
C:\Users\<Username>\AppData\Local\MyPOSApp\logs\
```

Проверьте лог файл:
```cmd
notepad "%LOCALAPPDATA%\MyPOSApp\logs\main.log"
```

В логе должны быть записи:
```
[2026-01-15 12:00:00.123] [info] MyPOSApp запущен успешно
[2026-01-15 12:00:00.234] [info] Автозапуск включен
```

---

## 🧪 Тестирование функционала

### 1️⃣ Тест большой таблицы (📊)

**Цель:** Проверка производительности рендеринга

1. Нажмите на плитку **"Большая таблица"**
2. В выпадающем списке выберите **15,000 (300 стр.)**
3. Проверьте:
   - ✅ Таблица загружается быстро (< 2 секунд)
   - ✅ Прокрутка плавная
   - ✅ Сортировка работает (клик на заголовок столбца)
   - ✅ Поиск работает (введите номер заказа)
   - ✅ Фильтрация по статусу работает

**Экстремальный тест:**
4. Выберите **50,000 (1000 стр.)**
5. Подождите ~5-10 секунд
6. Проверьте, что приложение не зависло

### 2️⃣ Тест вычислений (🧮)

**Цель:** Проверка CPU под нагрузкой

1. Вернитесь на главную (ссылка вверху)
2. Нажмите **"Сложные вычисления"**
3. Запустите все тесты:
   - Фибоначчи: должно быть **< 1 секунды**
   - Простые числа: **< 2 секунд**
   - Сортировка: **< 3 секунд**
   - Матрицы: **< 2 секунд**

⚠️ **Если время больше** — возможны проблемы с CPU ATOL

### 3️⃣ Тест Canvas (🎨)

**Цель:** Проверка GPU и рендеринга

1. Откройте **"Canvas анимации"**
2. Проверьте:
   - ✅ Частицы плавно двигаются (≥30 FPS)
   - ✅ Волны анимируются без рывков
   - ✅ Частотные полосы реагируют на движения

⚠️ **Если тормозит** — GPU может быть проблемой

### 4️⃣ Тест кухонного дисплея (👨‍🍳)

**Цель:** Проверка real-time обновлений

1. Откройте **"Кухонный экран"**
2. Нажмите **"Запустить симуляцию"**
3. Проверьте:
   - ✅ Заказы появляются каждые несколько секунд
   - ✅ Таймеры обновляются в реальном времени
   - ✅ Можно менять статус заказа (клик на кнопки)
   - ✅ Заказы удаляются при завершении

### 5️⃣ Тест клавиатуры (⌨️)

**Цель:** Проверка сенсорного ввода

1. Откройте **"Сенсорная клавиатура"**
2. Проверьте:
   - ✅ Клавиатура отображается
   - ✅ Клик по клавишам вводит текст
   - ✅ Переключение языков работает (RU/EN)
   - ✅ Цифровая клавиатура работает

### 6️⃣ Тест календаря (📅)

**Цель:** Проверка работы с датами

1. Откройте **"Календарь и датапикер"**
2. Проверьте:
   - ✅ Календари отображаются
   - ✅ Можно выбрать дату
   - ✅ Можно выбрать диапазон
   - ✅ События отображаются

### 7️⃣ Тест долгой работы (24/7) ⏱️

**Цель:** Проверка стабильности при длительной работе

1. Оставьте приложение работать **8-12 часов**
2. Периодически проверяйте:
   - ✅ Приложение не зависло
   - ✅ Память не растет бесконечно (Task Manager)
   - ✅ Интерфейс отзывчивый

**Проверка памяти:**
```cmd
# Откройте диспетчер задач
Ctrl + Shift + Esc

# Найдите MyPOSApp.exe
# Проверьте использование RAM
# Норма: 200-400 MB
# ⚠️ Проблема: > 1 GB
```

---

## 🔧 Устранение неполадок

### Проблема 1: Приложение не запускается

**Симптомы:** Двойной клик — ничего не происходит

**Решение:**
1. Проверьте логи:
   ```cmd
   notepad "%LOCALAPPDATA%\MyPOSApp\logs\main.log"
   ```
2. Ищите ошибки (строки с `[error]`)
3. Запустите от администратора:
   ```cmd
   # Правой кнопкой → Запуск от имени администратора
   ```

### Проблема 2: Черный экран

**Симптомы:** Приложение запускается, но экран черный

**Решение:**
```cmd
# Это проблема GPU, но мы её уже пофиксили в коде
# Если все равно черный экран:

# 1. Проверьте драйвера GPU
# 2. Перезагрузите ATOL
# 3. Переустановите приложение
```

### Проблема 3: Приложение тормозит

**Симптомы:** Медленная прокрутка, долгая загрузка

**Возможные причины:**
1. **Недостаточно RAM** — закройте другие программы
2. **CPU загружен** — проверьте Task Manager
3. **Диск переполнен** — освободите место

**Проверка:**
```cmd
# Откройте Task Manager
Ctrl + Shift + Esc

# Проверьте:
# - CPU: должен быть < 80%
# - RAM: должно быть свободно > 1 GB
# - Disk: должен быть < 100%
```

### Проблема 4: Приложение крашится

**Симптомы:** Приложение внезапно закрывается

**Решение:**
1. Проверьте логи перед крашем
2. **Watchdog должен автоматически перезапустить** приложение
3. Если не перезапускается — проверьте автозапуск:
   ```cmd
   # Win + R
   shell:startup
   # Должен быть ярлык MyPOSApp
   ```

### Проблема 5: Не работает автозапуск

**Симптомы:** После перезагрузки приложение не стартует

**Решение:**
1. Проверьте автозагрузку:
   ```cmd
   # Win + R
   shell:startup
   ```
2. Создайте ярлык вручную:
   - Скопируйте ярлык из рабочего стола
   - Вставьте в папку автозагрузки

---

## 🔐 Администрирование

### Как закрыть приложение (для администратора)

Приложение защищено от закрытия кассирами. Закрыть можно:

#### Способ 1: Task Manager
```cmd
# 1. Нажмите Ctrl + Shift + Esc
# 2. Найдите "MyPOSApp.exe"
# 3. Правой кнопкой → "Снять задачу"
```

#### Способ 2: Командная строка
```cmd
taskkill /IM MyPOSApp.exe /F
```

#### Способ 3: Выход из Kiosk режима (во время разработки)
Если нужно протестировать без Kiosk режима:
```cmd
# Перед запуском установите переменную
set NODE_ENV=development
"C:\Program Files\MyPOSApp\MyPOSApp.exe"
```

### Удаление приложения

#### Стандартное удаление:
1. **Панель управления** → **Программы и компоненты**
2. Найдите **"MyPOSApp"**
3. Нажмите **"Удалить"**

#### Полная очистка (с логами):
```cmd
# Удаление программы
"C:\Program Files\MyPOSApp\Uninstall MyPOSApp.exe"

# Удаление логов и настроек
rmdir /S /Q "%LOCALAPPDATA%\MyPOSApp"
```

### Обновление приложения

1. Удалите старую версию
2. Установите новую версию `MyPOSApp-Setup.exe`
3. Перезагрузите ATOL

⚠️ **Примечание:** Автообновления отключены для POS систем

---

## 📊 Мониторинг производительности

### Просмотр метрик в реальном времени

```cmd
# Откройте Performance Monitor
perfmon

# Добавьте счетчики:
# - Processor Time
# - Available Memory
# - Disk Queue Length
```

### Анализ логов

```cmd
# Последние 50 строк лога
powershell -command "Get-Content '%LOCALAPPDATA%\MyPOSApp\logs\main.log' -Tail 50"

# Поиск ошибок
findstr /i "error" "%LOCALAPPDATA%\MyPOSApp\logs\main.log"

# Поиск предупреждений
findstr /i "warn" "%LOCALAPPDATA%\MyPOSApp\logs\main.log"
```

---

## 📞 Поддержка

### При возникновении проблем:

1. **Соберите информацию:**
   - Скриншот ошибки
   - Лог файл из `%LOCALAPPDATA%\MyPOSApp\logs\`
   - Версия Windows (`winver`)
   - Характеристики ATOL (CPU, RAM)

2. **Создайте issue на GitHub:**
   - https://github.com/idrisovTamer/electron/issues

3. **Опишите проблему:**
   - Что делали?
   - Что ожидали?
   - Что получили?
   - Приложите логи и скриншоты

---

## ✅ Чеклист успешного развертывания

- [ ] Приложение собрано (`npm run package:win`)
- [ ] Установщик скопирован на ATOL
- [ ] Приложение установлено с правами администратора
- [ ] Первый запуск прошел успешно
- [ ] Fullscreen/Kiosk режим работает
- [ ] Все стресс-тесты пройдены
- [ ] Блокировка выхода работает (Alt+F4, крестик)
- [ ] DevTools заблокированы (F12)
- [ ] Автозапуск настроен
- [ ] Логи пишутся корректно
- [ ] Тест 8-12 часов пройден
- [ ] Watchdog тестирован (принудительный краш)

---

## 🎉 Готово!

Приложение готово к эксплуатации на ATOL OPTIMA v7!

**Удачного тестирования!** 🚀💰
